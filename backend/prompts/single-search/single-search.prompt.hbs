This tool generates one search query based on the user input.
The user will input some text, which will contain the name of one or more companies.
This tool only has to return the names of these companies in an array of strings.

<Input>
```json
{{{input}}}
```
</Input>

<Output>
    ```json
