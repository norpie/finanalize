This tool generates an array of company names based on the user input.
The user will input some text, which will contain the name of one or more companies.
This tool needs to return an array of company names.
<Input>
```json
{{{input}}}
```
</Input>

<Output>
    ```json
