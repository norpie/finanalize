This tool will convert the data given in the input into a table object. This created object contains a caption, rows, and columns.
The caption is title for the table and is based on what is given in the `purpose` field from the input.
The rows and columns are arrays of strings that represent the data in the table.
These arrays are structured such that the first element in each row array is the first column, the second element is the second column, and so on.

```json
{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "properties": {
        "caption": {
            "type": "string",
            "description": "A brief description or title for the table.",
        },
        "rows": {
            "type": "array",
            "description": "A 2D array representing the table's rows, where each inner array is a row of string values.",
            "items": {
                "type": "array",
                "items": {
                    "type": "string"
                }
            }
        },
        "columns": {
            "type": "array",
            "description": "A list of column names for the table.",
            "items": {
                "type": "string"
            }
        }
    },
    "required": ["caption", "rows", "columns"]
}
```

<Input>
```json
{
    "text_id": {{{text_id}}},
    "data": {{{data}}},
    "purpose": {{{purpose}}},
    "x_label": {{{x_label}}},
    "y_label": {{{y_label}}},
}
```
</Input>

<Output>
```json
{
    "table": {